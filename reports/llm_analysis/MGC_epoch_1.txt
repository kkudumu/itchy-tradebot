### REASONING

**The edge is short-only â€” longs are bleeding the account.**

| Strategy | Dir | WR | Avg R | Verdict |
|---|---|---|---|---|
| `ema_pullback` | short | 60.0% | +6.09 | **Star.** 234 wins from 390 entries. This alone funds the account. |
| `fx_at_one_glance` | short | 30.8% | +4.16 | **Profitable.** Low WR but outsized winners compensate. 507 entries = volume engine. |
| `ema_pullback` | long | 42.9% | -0.27 | **Net loser.** Nearly 43% WR should be profitable but avg R is negative â€” stops are too far from entry (slow-EMA anchor below price). The 2x ATR cap from the prior commit hasn't fixed this. |
| `fx_at_one_glance` | long | 50.0% | -0.40 | **Net loser.** 50% WR and still negative avg R â€” the wins are smaller than the losses. Only 78 entries but each one drags. |

**HTF Trend column shows "?" for every row.** The `block_without_data` flag was added in the prior commit, but the `should_allow()` method in `trend_direction.py` never reads it â€” when trend data is missing it unconditionally returns `allowed=True`. The flag is a dead field. This is the root cause: longs pass through unfiltered because the gate has no teeth.

**ichimoku, sss, asian_breakout** generated zero signals on MGC across 196 trials. They're consuming warmup bars and compute for nothing.

**54.6% combine pass rate with 19 avg trades** â€” cutting longs removes ~351 losing entries (273 + 78) while keeping ~897 profitable short entries. Fewer trades but higher expectancy should push pass rate above 60%.

### CONFIG_CHANGES
```json
{"config_changes": {"edges.trend_direction.enabled": true, "edges.trend_direction.params.block_without_data": true, "strategies.ichimoku.enabled": false, "strategies.sss.enabled": false, "strategies.asian_breakout.enabled": false, "strategies.ema_pullback.weight": 1.5}}
```

### CODE_PATCHES
```json
{"code_patches": [{"file": "src/edges/trend_direction.py", "description": "Actually use _block_without_data: when True and no HTF data available, block longs (net losers on MGC) but allow shorts (the proven edge). Currently the flag is set but never checked, so longs pass through unfiltered.", "search": "        if trend_1h is None and trend_4h is None:\n            return EdgeResult(\n                allowed=True,\n                edge_name=self.name,\n                reason=\"no HTF trend data â€” filter skipped\",\n            )", "replace": "        if trend_1h is None and trend_4h is None:\n            if self._block_without_data and direction == \"long\":\n                return EdgeResult(\n                    allowed=False,\n                    edge_name=self.name,\n                    reason=\"long blocked: no HTF trend data and block_without_data=True\",\n                )\n            return EdgeResult(\n                allowed=True,\n                edge_name=self.name,\n                reason=\"no HTF trend data â€” filter skipped\",\n            )"}]}
```
